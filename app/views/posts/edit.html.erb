<% provide(:title, "Edit Post #{@post.title}") %>

<%= form_for @post do |f| %>
  <%= f.label :Title %>
  <%= f.text_field :title %>
  <br>
  <%= f.label :Content %>
  <%= f.text_area :content %>
  <br>

  <%= f.submit %>
<% end %>
